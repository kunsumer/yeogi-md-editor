# Active spec

- `docs/claude-code/specs/2026-04-21-markdown-editor/` — macOS Markdown Editor (v1). **Status: shipped through v0.4.8** (2026-05-04). The core spec — folder explorer, side-by-side panes, WYSIWYG ⇄ Edit, wiki-links, autosave, conflict reconciliation, native menus — is in place and stable. Subsequent releases (v0.4.x) added drag-reorder tabs, right-click file management, per-document Reload from disk, and stable code-signing in CI.

No active spec in flight. The next deliberately-scoped feature should get its own dated folder under `docs/claude-code/specs/`.
