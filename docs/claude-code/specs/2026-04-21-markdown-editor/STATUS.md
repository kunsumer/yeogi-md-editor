# STATUS — macOS Markdown Editor (v1)

## Current state

Phases 0 and 1 of PLAN.md complete. On `main` branch, HEAD `7b6cf94`.

- Git repo initialized, project moved to `/Users/peter/Documents/evhan-md-editor/` (old path is a symlink).
- Tauri 2 + Vite + React 18 + TypeScript scaffolded.
- Vitest + Playwright + GitHub Actions CI wired.
- Rust backend has: `types.rs`, `fs.rs` (read / write / create / rename / list), `commands.rs` (5 `#[tauri::command]` wrappers), all registered in `lib.rs::run()`. `main.rs` is a thin shim.
- 15 tests green (`cargo test`): 10 fs integration + 5 ts-rs export-bindings.
- `src/lib/ipc/types.ts` generated by ts-rs.

## In progress

None. Ready to resume at **Phase 2 — Rust file watcher** (2 tasks: watcher module + wire events into `lib.rs::run()`).

## Decisions carried forward (read before resuming)

- **Execution mode:** `superpowers:subagent-driven-development` with a pragmatic cadence — full three-subagent flow (implementer + spec review + code quality review) for tasks with real code or architectural touches; inline-by-controller for verbatim config / YAML / single-flag fixes.
- **Architecture override to PLAN.md:** `main.rs` stays a thin caller; the Tauri `Builder` lives in `lib.rs::run()`. All plan text that says "rewrite `main.rs`" means "update `lib.rs::run()`". See the Phase 1 preamble added in commit `6141a6b`.
- **bigint vs number:** ts-rs 8 emits `mtime_ms: bigint` in the generated `src/lib/ipc/types.ts`. At runtime the wire value is a plain JSON number. Phase 3 IPC wrappers coerce at the boundary and re-export cleaned types with `number`. See the Phase 3 preamble added in commit `d0bc1cc`.
- **Stray ts-rs output:** `src-tauri/src/lib/ipc/types.ts` is gitignored (prevents re-commit on `cargo test`).
- **`@types/react` / `@types/react-dom`** pinned to `^18` to match runtime.
- **`vitest run --passWithNoTests`** — required so CI stays green until the first TS test lands in Phase 3.
- **`"csp": null`** in `tauri.conf.json` — deferred hardening for Phase 8 when the markdown renderer lands.

## Risks

See `SPEC.md` → Risks. No new risks uncovered in Phases 0–1 beyond the ts-rs 8 bigint quirk already handled.

## Next milestone

Phase 2 (watcher + event wiring). Then Phase 0-0.5 checkpoint criteria repeat for Phase 2: all tests pass, `cargo build` clean, `pnpm tauri dev` launches. Then Phase 3 — frontend document model, IPC wrappers, conflict function (where the `bigint → number` coercion gets implemented).

## How to resume

In a new Claude Code session at `/Users/peter/Documents/evhan-md-editor/`, say: `Resume the implementation plan at docs/claude-code/specs/2026-04-21-markdown-editor/PLAN.md. We're at Phase 2. Read this STATUS.md first.`
