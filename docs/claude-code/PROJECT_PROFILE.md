# PROJECT_PROFILE

Fill in once a stack is chosen. This file drives all later decisions.

## App name
<name>

## Target
- platform: macOS
- minimum OS version:
- distribution: (direct download / notarized / Mac App Store / TestFlight / unsigned dev)

## What this repo owns
- windows / scenes:
- file access scope: (open/save panels only, bookmark-based, full-disk)
- file watching: (yes / no)
- markdown parser:
- syntax highlighting:
- preview renderer:

## Stack
- language:
- UI framework: (SwiftUI / AppKit / Tauri + web / Electron + web)
- editor component: (TextKit / CodeMirror / Monaco / other)
- state / data library:
- test framework:
- UI / integration test framework:
- lint / formatter:
- build / packaging tool:

## Real commands
- install:
- dev (run app locally):
- build (release / .app bundle):
- lint:
- typecheck:
- unit test:
- integration / UI test:
- sign / notarize:

## Risk zones
- file I/O and unsaved changes:
- destructive actions (delete / overwrite / move):
- sandbox / entitlements:
- large-file handling:
- accessibility:
- crash on unsaved state:
